kind: PersistentVolume
apiVersion: v1
metadata:
  name: redis-tinyurl-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/redis"
